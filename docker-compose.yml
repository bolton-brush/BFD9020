services:
  api:
    image: ghcr.io/open-ortho/edu.case.bfd9020:local
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9020:9020"
    environment:
      LOG_LEVEL: "INFO"
      ROOT_PATH: "/bfd9020"
